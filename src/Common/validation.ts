// import Joi from 'react-native-joi';

// export const mobileRegister = Joi.object().keys({
//   affiliation: Joi.string()
//     .valid('army', 'airforce', 'navy', 'marine', 'rok')
//     .lowercase().required(),
//   code: Joi.string().length(6).regex(/^\d+$/),
//   name: Joi.string()
//     .regex(/^[가-힣]{2,8}$/).required(),
//   msn: Joi.string().length(10).regex(/^\d+$/).required(),
// });  //role은 soldier로 고정

export const mobileRegister = "a"

